import { dictionary as ptDictionary } from "@/i18n/dictionaries/pt";
import type { Locale } from "@/i18n/settings";

export const localePt: Locale = "pt";

export const headerFixture = ptDictionary.header;
export const heroFixture = ptDictionary.hero;
export const languageSwitcherFixture = ptDictionary.languageSwitcher;
export const navFixture = ptDictionary.nav;
